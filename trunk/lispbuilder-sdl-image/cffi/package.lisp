;;; -*- lisp -*-

(in-package #:cl-user)

(defpackage #:lispbuilder-sdl-image-cffi
  (:use #:cl #:cffi)
  (:nicknames #:sdl-image-cffi)
  (:documentation "The main package of `lispbuilder-sdl-image'.")
  (:export

   ;; No symbols are exported from the CFFI package.
))
