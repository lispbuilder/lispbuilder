
(in-package :lispbuilder-sdl-gfx)

;; (defvar *default-font* nil
;;   "When a `LISPBUILDER-SDL-GFX` function or macro has an `OPTIONAL` or `KEY`word argument 
;; `FONT`, the default action is to bind `FONT` to `\*DEFAULT-FONT\*`.`\*DEFAULT-FONT\*` is set to `NIL` initially. 
;; A font must be initialized by calling [INITIALISE-DEFAULT-FONT](#initialise-default-font) prior to use.
;; This will bind `FONT` to `\*DEFAULT-FONT\*`.")
